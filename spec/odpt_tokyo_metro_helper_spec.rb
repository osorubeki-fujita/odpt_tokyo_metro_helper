require 'spec_helper'

describe OdptTokyoMetroHelper do
  it 'has a version number' do
    expect(OdptTokyoMetroHelper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
